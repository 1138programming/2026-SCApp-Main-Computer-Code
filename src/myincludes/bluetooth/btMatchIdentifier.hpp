#ifndef BT_MATCH_IDENTIFIER_HPP
#define BT_MATCH_IDENTIFIER_HPP

#include <string>

namespace bt {
    struct MatchIdentifier {
        std::string comp;
        std::string team;
        std::string match;
    };
}

#endif